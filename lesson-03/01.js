// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое

function calculateAverage(first, second) {
	let average = (first + second)/2;
	return average;
}
let average = calculateAverage(3,4)
console.log(average)